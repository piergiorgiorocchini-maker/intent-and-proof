{
  "name": "intent-and-proof",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "node scripts/validate-sveltia-admin.mjs && bash -o pipefail -c 'astro build 2>&1 | tail -n 100'",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@fontsource-variable/inter": "^5.3.0",
    "@fontsource-variable/manrope": "^5.3.0",
    "astro": "^7.0.4"
  }
}